# Verifier Specification

The URF Verifier validates certificate structure, declared scope,
provenance-facing artifacts, and deterministic verification inputs.

## Current scope

- schema validation
- deterministic verifier entrypoints
- signature/provenance-facing surfaces
- scope-limited documentation

## Out of scope

- mathematical promotion beyond verifier scope
- unsupported artifact formats
- unstated certificate semantics
