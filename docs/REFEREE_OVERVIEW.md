# URF Core Verifier – Referee Overview

This repository provides an executable verifier for URF Core objects.
It is deterministic and outputs JSON results that can be incorporated
into CI workflows and artifact validation pipelines.

The verifier does not alter URF Core semantics; it only checks them.
